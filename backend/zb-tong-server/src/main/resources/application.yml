server:
  port: 8080
  servlet:
    context-path: /api

spring:
  profiles:
    active: prod
  application:
    name: zb-tong-server

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zb_tong?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000
      max-lifetime: 1200000
      connection-timeout: 30000

  redis:
    host: localhost
    port: 6379
    password: ${REDIS_PASSWORD}
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    table: flyway_schema_history
    encoding: utf-8

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.zhubao.manage
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: isDeleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      update-strategy: not_null
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

knife4j:
  enable: true
  setting:
    language: zh-CN

jwt:
  secret: ${JWT_SECRET:}  # 生产无默认值，空密钥导致启动失败；dev 环境在 application-dev.yml 覆盖
  expiration: 86400000  # 1天
  header: Authorization
  token-prefix: "Bearer "

file:
  storage:
    type: local  # local(本地磁盘,无需外部服务) | minio | oss
    pre-url-expiry: 600
    minio:
      endpoint: http://localhost:9000
      access-key: ${MINIO_ACCESS_KEY}
      secret-key: ${MINIO_SECRET_KEY}
      bucket: zbtong

xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    accessToken: ${XXL_ACCESS_TOKEN}
    executor:
      appname: zb-tong-executor
      address:
      ip:
      port: 9999
      logpath: logs/xxl-job
      logretentiondays: 30

ai:
  openai:
    api-key: ${OPENAI_API_KEY}
    model: ${AI_MODEL:gpt-4}
    base-url: ${AI_BASE_URL:https://api.openai.com/v1}
    timeout: 120

# 高德地图（C端新增地址定位：逆地理编码）
amap:
  key: ${AMAP_KEY:}

logging:
  level:
    root: INFO
    com.zhubao.manage: INFO
