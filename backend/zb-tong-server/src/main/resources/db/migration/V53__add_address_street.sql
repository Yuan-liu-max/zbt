-- ============================================================
-- V53: 收货地址增加街道字段（淘宝式布局：省市区+街道一栏，门牌号单独一栏）
-- ============================================================

ALTER TABLE `user_address`
  ADD COLUMN `street` varchar(100) DEFAULT NULL COMMENT '街道/乡镇' AFTER `district`;
