# 全链路修复任务清单

基于审查报告，请按优先级修复以下问题。

---

## 🚨 P0 — 阻塞性问题

### 1. 9 个 Entity 添加 @TableLogic isDeleted

以下 Entity 对应的数据库表有 `is_deleted` 列，但 Entity 缺少 `@TableLogic` 字段，会导致逻辑删除变成物理删除：

| Entity 文件 | 对应表 |
|------------|--------|
| `module/report/entity/StoreMonthlyScore.java` | store_monthly_score |
| `module/report/entity/ReportSnapshot.java` | report_snapshot |
| `module/report/entity/OperateLog.java` | operate_log |
| `module/order/entity/OrderItem.java` | order_item |
| `module/order/entity/OrderLog.java` | order_log |
| `module/order/entity/OrderReturn.java` | order_return |
| `module/customer/entity/MemberLevel.java` | member_level |
| `module/purchase/entity/PurchaseItem.java` | purchase_item |
| `module/task/entity/TaskAudit.java` | task_audit |

在每个类中添加：
```java
@TableLogic
private Integer isDeleted;
```

### 2. ProductSalesAnalysis.noSales7Days 列名映射错误

文件：`module/product/entity/ProductSalesAnalysis.java`

Entity 字段名 `noSales7Days` 经过驼峰转换后是 `no_sales7_days`，
但数据库实际列名是 `no_sales_7_days`（数字 7 前后有下划线）。

加上显式映射：
```java
@TableField("no_sales_7_days")
private String noSales7Days;
```

### 3. 修复 V21 迁移脚本重复 image_url 列

文件：`db/migration/V21__product_stock_brand_fields.sql`

V4 建表时已有 `image_url`，V21 再次 ADD COLUMN 会报错。删除 V21 中的：
```sql
ALTER TABLE `product` ADD COLUMN `image_url` VARCHAR(500) NULL COMMENT '商品图片';
```

---

## ⚠️ P1 — 安全风险

### 4. Shop 商城改用 /shop/auth 专用端点

Shop 前端 `shop/src/api/auth.ts` 当前调用 `/auth/login`，应改为 `/shop/auth/login`。

后端已有 `ShopAuthController`（`/shop/auth`），它会对 CUSTOMER 角色做校验，防止管理后台用户通过商城登录。

修改 `shop/src/api/auth.ts`，将所有 `/auth/` 替换为 `/shop/auth/`：
- `/auth/login` → `/shop/auth/login`
- `/auth/register` → `/shop/auth/register`  
- `/auth/me` → `/shop/auth/me`
- `/auth/logout` → `/shop/auth/logout`
- `/auth/profile` → `/shop/auth/profile`
- `/auth/password` → `/shop/auth/password`
- `/auth/account` → `/shop/auth/account`（或检查 ShopAuthController 是否有此端点）

### 5. ProductController 内部运营端点添加 @PreAuthorize

文件：`module/product/controller/ProductController.java`

以下端点当前无认证保护，应添加 `@PreAuthorize("isAuthenticated()")`：
- `/inventory-checks` 全部 5 个端点
- `/maintenance-checks` 全部 5 个端点
- `/sales-analyses` 全部 5 个端点
- `/new-product-plans` 全部 5 个端点
- `/promotion-plans` 全部 5 个端点
- `/inventory-warnings` 全部 3 个端点
- `/{id}/adjust-stock`
- `/transfer`

### 6. 登出后 Token 失效

在 `application-dev.yml` 中添加：
```yaml
spring:
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude:
        - static/**
        - public/**
        - META-INF/**
    livereload:
      enabled: false
  data:
    redis:
      host: localhost
      port: 6379
```

然后实现 Redis Token 黑名单：
- `AuthController.logout()` 将当前 Token 加入 Redis 黑名单，TTL 与 Token 过期时间一致
- `JwtAuthenticationFilter` 在解析 Token 前检查 Redis 黑名单

---

## 🔵 P2 — 代码质量

### 7. ProductController 拆分子域 Controller

当前 ProductController 承担了 43 个端点。建议按子域拆分：
- `ProductController` — 仅保留商品 CRUD（9 个端点）
- `InventoryCheckController` — `/inventory-checks`（5 个端点）
- `MaintenanceCheckController` — `/maintenance-checks`（5 个端点）
- `SalesAnalysisController` — `/sales-analyses`（5 个端点）
- `NewProductPlanController` — `/new-product-plans`（5 个端点）
- `PromotionPlanController` — `/promotion-plans`（5 个端点）
- `InventoryWarningController` — `/inventory-warnings`（3 个端点）
- 库存调整/调拨保留在 `ProductController`

### 8. 数据库 VARCHAR 日期字段改为原生类型

将以下表中的 VARCHAR 日期字段改为 DATETIME/DATE：
- `customer.registered_at` VARCHAR(20) → DATETIME
- `customer.last_consumption_at` VARCHAR(20) → DATETIME
- `certificate.issue_date` VARCHAR(20) → DATE
- `certificate.expiry_date` VARCHAR(20) → DATE
- `marketing_activity.start_time` VARCHAR(20) → DATETIME
- `marketing_activity.end_time` VARCHAR(20) → DATETIME
- `promotion.start_time` VARCHAR(20) → DATETIME
- `promotion.end_time` VARCHAR(20) → DATETIME
- `transaction.transaction_date` VARCHAR(20) → DATETIME
- `employee_meeting.meeting_date` VARCHAR(20) → DATE
- `order_log.time` VARCHAR(20) → DATETIME
- `order_return.apply_time` VARCHAR(20) → DATETIME

创建新迁移脚本执行 ALTER TABLE。

### 9. JwtAuthenticationFilter 角色缓存

当前每次请求执行 2 次 DB 查询获取角色。优化方案：
- 在 JWT claims 中加入 `roles` 字段（Token 生成时写入）
- 或使用 Redis 缓存 `userId → roles`，TTL 与 Token 一致

### 10. Shop 商城 401 跳转登录页

在 `shop/src/api/index.ts` 的响应拦截器中，401 时添加路由跳转：
```typescript
if (status === 401) {
  localStorage.removeItem('zbt_token')
  localStorage.removeItem('zbt_user')
  showToast('登录已过期，请重新登录')
  router.push('/login')  // ← 加上这一行
}
```

---

## 执行顺序

```
P0 → 先修复数据库和 ORM 层面的错误（否则数据会丢失）
P1 → 修复安全和权限问题
P2 → 优化代码质量和性能
```
