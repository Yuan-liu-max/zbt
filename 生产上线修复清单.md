# 生产上线修复清单

> 按顺序逐项修复，每项修复后可独立验证。建议按 🔴 → 🟡 → 🔵 顺序执行。

---

## 🔴 第一批：阻塞上线（必须修复）

### ☐ 1. 新建 application-prod.yml（后端生产配置）

**文件**：`backend/zb-tong-server/src/main/resources/application-prod.yml`（新建）

**内容要点**：
```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/zb_tong?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  redis:
    host: ${REDIS_HOST:localhost}
    password: ${REDIS_PASSWORD}
  flyway:
    enabled: false        # 生产手动初始化，避免种子账号写入

knife4j:
  enable: false           # 关闭 API 文档

logging:
  level:
    root: INFO
    com.zhubao.manage: INFO   # 关 DEBUG
```

**验证**：`java -jar xxx.jar --spring.profiles.active=prod` 启动后，knife4j 访问 `/api/doc.html` 应 404。

---

### ☐ 2. 修改默认 profile 和 JWT 密钥

**文件**：`application.yml`

**修改**：
- 第 8 行 `spring.profiles.active: dev` → 删除或改为 `prod`
- 第 78 行 `jwt.secret: ${JWT_SECRET:}` 保持空默认值（生产必须注入）

**验证**：不指定 profile 启动时，默认用 prod 而非 dev。

---

### ☐ 3. 删除默认 admin/admin123 和测试账号

**文件**：
- `V8__init_seed_data.sql:187-189`（admin 账号）
- `V37__shop_seed_data.sql`、`V38__fix_seed_passwords.sql`（customer1/customer2）

**修改**：删除或注释掉这些种子账号的 INSERT 语句。

**验证**：生产库初始化后，`sys_user` 表没有 admin123/customer1 等弱口令账号。

---

### ☐ 4. Cookie 加 Secure 标志

**文件**：
- `AuthController.java:76,81`
- `ShopAuthController.java:337,342`

**修改**：Cookie 字符串加 `; Secure`：
```java
"zbt_admin_token=%s; HttpOnly; Secure; Path=/; Max-Age=259200; SameSite=Lax"
```

**验证**：登录后查看 Set-Cookie 响应头，包含 `Secure` 标志。

---

### ☐ 5. 修复 AI API Key 日志泄露

**文件**：`AIAnalysisJob.java:56`

**修改**：
```java
// 改前
log.warn("[AI SKIP] OPENAI_API_KEY 未配置 (当前值: {}), 跳过AI分析", apiKey);
// 改后
log.warn("[AI SKIP] OPENAI_API_KEY 未配置，跳过AI分析");
```

**验证**：日志中不再出现 apiKey 明文。

---

### ☐ 6. 编写 Nginx 反向代理配置（最关键）

**文件**：`deploy/nginx.conf`（新建）

**内容**：
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    # 管理端静态托管
    location /admin/ {
        alias /var/www/frontend/dist/;
        try_files $uri $uri/ /admin/index.html;
    }
    # 商城静态托管
    location /h5/ {
        alias /var/www/shop/dist/;
        try_files $uri $uri/ /h5/index.html;
    }
    # 后端 API 反代（注意后端 context-path 是 /api）
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /files/ {
        proxy_pass http://127.0.0.1:8080/api/files/;
    }
    location /images/ {
        proxy_pass http://127.0.0.1:8080/api/images/;
    }
}
```

**验证**：部署后，前端页面能正常调 API、显示图片、上传文件。

---

### ☐ 7. 前端配置 base（子路径部署）

**文件**：
- `frontend/vite.config.ts` → `base: '/admin/'`
- `shop/vite.config.ts` → `base: '/h5/'`

**修改**：在 `defineConfig` 里加 `base` 字段。

**验证**：`npm run build` 后，`dist/index.html` 里的资源路径是 `/admin/assets/...` 而非 `/assets/...`。

---

## 🟡 第二批：重要加固

### ☐ 8. 关闭 SQL stdout 输出和 DEBUG 日志

**文件**：`application.yml`

**修改**：
- 第 70 行 `log-impl: org.apache.ibatis.logging.stdout.StdOutImpl` → 删除或改为 `org.apache.ibatis.logging.slf4j.Slf4jImpl`
- 第 116 行 `com.zhubao.manage: DEBUG` → `INFO`

**验证**：生产启动后控制台不再打印每条 SQL。

---

### ☐ 9. 登录加暴力破解防护

**文件**：`AuthController.java` + `ShopAuthController.java`（或新写登录拦截）

**方案**：登录失败计数 + 锁定（Redis 记录失败次数，5 次锁定 15 分钟）。

**验证**：连续输错 5 次密码后，账号被临时锁定。

---

### ☐ 10. 关闭 /auth/register 开放注册

**文件**：`SecurityConfig.java:68`、`WebMvcConfig.java:27-28`

**修改**：移除 `/auth/register` 的 permitAll，改为仅管理员可用或直接关闭。

**验证**：未登录访问 `/api/auth/register` 返回 401。

---

### ☐ 11. 数据库启用 SSL

**文件**：`application.yml:14`

**修改**：`useSSL=false` → `useSSL=true`，删除 `allowPublicKeyRetrieval=true`。

**验证**：生产环境数据库连接走 SSL。

---

### ☐ 12. 缩短 JWT 过期时间

**文件**：`application.yml:79`

**修改**：`expiration: 259200000`（3天）→ `86400000`（1天）或更短。

**验证**：Token 1 天后过期。

---

### ☐ 13. 统一图片 URL 前缀

**文件**：
- `frontend/src/views/goods/GoodsList.vue:81,229`
- `frontend/src/views/goods/GoodsBrand.vue:56`
- `frontend/src/utils/avatar.ts`

**修改**：统一用 `/api/files/static/...` 或统一用 `/files/static/...`（与 Nginx 代理规则对齐）。

**验证**：所有图片 URL 前缀一致，Nginx 只需配一个规则。

---

## 🔵 第三批：优化建议

### ☐ 14. 移除外部占位图依赖
`shop/src/views/Home.vue:177-181`、`Product.vue:115-117` 的 `placehold.co` 换成本地占位图。

### ☐ 15. 清理脚手架残留
删除 `frontend/src/components/HelloWorld.vue`、`frontend/src/assets/vue.svg`、`vite.svg`。

### ☐ 16. 补操作日志注解
给核心写操作 Controller 方法标注 `@OperateLog`（否则日志管理板块永远为空）。

### ☐ 17. 前端环境变量隔离
添加 `.env.development` / `.env.production`，用 `import.meta.env.VITE_API_BASE_URL`。

### ☐ 18. 评估依赖升级
Spring Boot 2.7.18 已 EOL，评估升级到 3.x。

---

## 验证清单（全部完成后）

- [ ] 后端以 prod profile 启动，无 DEBUG 日志、无 SQL 输出
- [ ] knife4j 文档不可访问
- [ ] 登录后 Cookie 有 Secure 标志
- [ ] Nginx 反代生效，前端能正常访问 API、显示图片、上传文件
- [ ] admin 默认密码已修改，测试账号已删除
- [ ] 日志中无 apiKey、密码等敏感信息
